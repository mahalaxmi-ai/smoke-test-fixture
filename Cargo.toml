[package]
name = "manifest-validator"
version = "0.1.0"
edition = "2021"
description = "Validates requirement manifest JSON files, including circular dependency detection"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
members = ["fixture-crate"]
resolver = "2"
